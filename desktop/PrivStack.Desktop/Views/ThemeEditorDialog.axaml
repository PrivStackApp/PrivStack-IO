<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:PrivStack.Desktop.ViewModels"
             xmlns:views="using:PrivStack.Desktop.Views"
             mc:Ignorable="d" d:DesignWidth="1400" d:DesignHeight="850"
             x:Class="PrivStack.Desktop.Views.ThemeEditorDialog"
             x:DataType="vm:ThemeEditorViewModel"
             IsVisible="{Binding IsOpen, FallbackValue=False}">

    <Panel>
        <!-- Semi-transparent backdrop -->
        <Border Classes="modal-backdrop"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                PointerPressed="OnBackdropPressed"/>

        <!-- Dialog Container: stretches to fill window with comfortable margins -->
        <Border HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Margin="40,28"
                MinWidth="960"
                Background="{DynamicResource ThemeSurfaceElevatedBrush}"
                BorderBrush="{DynamicResource ThemeBorderBrush}"
                BorderThickness="1"
                CornerRadius="12"
                Classes="modal"
                ClipToBounds="True">

            <Grid RowDefinitions="Auto,*">
                <!-- Header -->
                <Border Grid.Row="0" Padding="24,16,24,14"
                        BorderBrush="{DynamicResource ThemeBorderBrush}"
                        BorderThickness="0,0,0,1">
                    <Grid RowDefinitions="Auto,Auto">
                        <!-- Top row: Title + action buttons -->
                        <Grid Grid.Row="0" ColumnDefinitions="Auto,*,Auto" Margin="0,0,0,12">
                            <StackPanel Grid.Column="0" Spacing="4">
                                <TextBlock Text="Theme Editor"
                                           FontSize="{DynamicResource ThemeFontSizeLg}"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource ThemeTextPrimaryBrush}"/>
                                <TextBlock Text="Customize colors and preview every token state in real time"
                                           FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                           Foreground="{DynamicResource ThemeTextMutedBrush}"/>
                            </StackPanel>

                            <!-- Action buttons -->
                            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8"
                                        VerticalAlignment="Top">
                                <Button Content="Reset All"
                                        Command="{Binding ResetToDefaultCommand}"
                                        Padding="12,6"
                                        FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                        ToolTip.Tip="Reset all colors to base theme defaults"/>
                                <Button Content="Delete"
                                        Command="{Binding DeleteCommand}"
                                        IsVisible="{Binding CanDelete}"
                                        Background="{DynamicResource ThemeDangerBrush}"
                                        Foreground="{DynamicResource ThemeTextOnAccentBrush}"
                                        Padding="12,6"
                                        FontSize="{DynamicResource ThemeFontSizeXsSm}"/>
                                <Button Content="Cancel"
                                        Command="{Binding CancelCommand}"
                                        Padding="12,6"
                                        FontSize="{DynamicResource ThemeFontSizeXsSm}"/>
                                <Button Content="Save Theme"
                                        Command="{Binding SaveCommand}"
                                        Background="{DynamicResource ThemePrimaryBrush}"
                                        Foreground="{DynamicResource ThemeTextOnAccentBrush}"
                                        Padding="16,6"
                                        FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                        FontWeight="SemiBold"/>
                            </StackPanel>
                        </Grid>

                        <!-- Bottom row: Theme name + variant toggle -->
                        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="16">
                            <TextBox Text="{Binding ThemeName}"
                                     Watermark="Theme Name"
                                     Width="240" Padding="10,7"
                                     FontSize="{DynamicResource ThemeFontSizeSm}"/>
                            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                <TextBlock Text="Light variant"
                                           FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                           Foreground="{DynamicResource ThemeTextMutedBrush}"
                                           VerticalAlignment="Center"/>
                                <ToggleSwitch IsChecked="{Binding IsLightVariant}"
                                              OnContent="" OffContent=""
                                              VerticalAlignment="Center" Margin="0,-2,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Body: Colors (left, fixed) + Preview (right, fills remaining) -->
                <Grid Grid.Row="1" ColumnDefinitions="540,*">

                    <!-- Left column: Color groups -->
                    <Border Grid.Column="0"
                            BorderBrush="{DynamicResource ThemeBorderBrush}"
                            BorderThickness="0,0,1,0">
                        <ScrollViewer VerticalScrollBarVisibility="Auto"
                                      HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Spacing="4" Margin="16,12,16,16">
                                <ItemsControl ItemsSource="{Binding ColorGroups}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:ThemeColorGroup">
                                            <StackPanel Spacing="2" Margin="0,0,0,12">
                                                <!-- Group header -->
                                                <TextBlock Text="{Binding Name}"
                                                           FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                                           FontWeight="SemiBold"
                                                           Foreground="{DynamicResource ThemeTextMutedBrush}"
                                                           Margin="0,0,0,6"/>

                                                <!-- Color items -->
                                                <ItemsControl ItemsSource="{Binding Items}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate x:DataType="vm:ThemeColorItem">
                                                            <Grid ColumnDefinitions="Auto,*,Auto,Auto"
                                                                  Margin="0,1" Height="30">
                                                                <!-- Color swatch button with flyout -->
                                                                <Button Grid.Column="0"
                                                                        Width="24" Height="24"
                                                                        Padding="0"
                                                                        CornerRadius="4"
                                                                        BorderBrush="{DynamicResource ThemeBorderBrush}"
                                                                        BorderThickness="1"
                                                                        VerticalAlignment="Center"
                                                                        Cursor="Hand">
                                                                    <Button.Background>
                                                                        <SolidColorBrush Color="{Binding Color}"/>
                                                                    </Button.Background>
                                                                    <Button.Styles>
                                                                        <Style Selector="Button:pointerover /template/ ContentPresenter">
                                                                            <Setter Property="Background">
                                                                                <Setter.Value>
                                                                                    <SolidColorBrush Color="{Binding Color}"/>
                                                                                </Setter.Value>
                                                                            </Setter>
                                                                        </Style>
                                                                    </Button.Styles>
                                                                    <Button.Flyout>
                                                                        <Flyout Placement="RightEdgeAlignedTop">
                                                                            <Border Padding="8">
                                                                                <ColorView Color="{Binding Color}"
                                                                                           IsAlphaVisible="True"
                                                                                           IsColorSpectrumVisible="True"
                                                                                           IsColorComponentsVisible="True"
                                                                                           IsHexInputVisible="True"
                                                                                           IsColorPaletteVisible="False"/>
                                                                            </Border>
                                                                        </Flyout>
                                                                    </Button.Flyout>
                                                                </Button>

                                                                <!-- Display name -->
                                                                <TextBlock Grid.Column="1"
                                                                           Text="{Binding DisplayName}"
                                                                           FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                                                           Foreground="{DynamicResource ThemeTextSecondaryBrush}"
                                                                           VerticalAlignment="Center"
                                                                           Margin="8,0,0,0"
                                                                           TextTrimming="CharacterEllipsis"/>

                                                                <!-- Hex input -->
                                                                <TextBox Grid.Column="2"
                                                                         Text="{Binding HexText}"
                                                                         Width="100"
                                                                         FontSize="{DynamicResource ThemeFontSizeXs}"
                                                                         FontFamily="{DynamicResource ThemeFontMono}"
                                                                         Padding="4,3"
                                                                         VerticalAlignment="Center"
                                                                         Margin="4,0"/>

                                                                <!-- Reset per-color -->
                                                                <Button Grid.Column="3"
                                                                        Command="{Binding ResetToDefaultCommand}"
                                                                        Background="Transparent"
                                                                        Padding="4,2"
                                                                        Cursor="Hand"
                                                                        VerticalAlignment="Center"
                                                                        ToolTip.Tip="Reset to default">
                                                                    <Button.Styles>
                                                                        <Style Selector="Button:pointerover /template/ ContentPresenter">
                                                                            <Setter Property="Background" Value="{DynamicResource ThemeHoverBrush}"/>
                                                                        </Style>
                                                                    </Button.Styles>
                                                                    <TextBlock Text="↺"
                                                                               FontSize="{DynamicResource ThemeFontSizeXsSm}"
                                                                               Foreground="{DynamicResource ThemeTextMutedBrush}"/>
                                                                </Button>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </ScrollViewer>
                    </Border>

                    <!-- Right column: Live preview -->
                    <Grid Grid.Column="1">
                        <Border Background="{DynamicResource ThemeBackgroundBrush}">
                            <views:ThemePreviewPanel/>
                        </Border>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Panel>
</UserControl>
