# Rust formatting configuration for PrivStack
# Using only stable rustfmt options

edition = "2021"
max_width = 100
tab_spaces = 4
newline_style = "Auto"
use_small_heuristics = "Default"

# Imports (stable options only)
reorder_imports = true
reorder_modules = true

# Functions
fn_params_layout = "Tall"
