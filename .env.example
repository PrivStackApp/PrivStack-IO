# OAuth provider credentials — OPTIONAL overrides for development.
# The app ships with compiled-in defaults for native PKCE/device-flow client IDs.
# Set these only if you need to use different OAuth apps (e.g., custom deployments).
# Copy this file to .env and fill in values to override.
PRIVSTACK_GOOGLE_CLIENT_ID=
PRIVSTACK_GOOGLE_CLIENT_SECRET=
PRIVSTACK_MICROSOFT_CLIENT_ID=
PRIVSTACK_GITHUB_CLIENT_ID=
